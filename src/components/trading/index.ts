/**
 * Trading UI — practice uses shared building blocks under `shared/`.
 *
 * `shared/` — TradePanel, mobile trade UI, modals, header chrome, news, DOM
 * `Practice/` — practice hub + session page wrappers
 * `Trading/` — TradeHeader, TradingRenderer
 */

export * from './shared'
export * from './modes/practice'
